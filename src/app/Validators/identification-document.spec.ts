import { IdentificationDocument } from './identification-document';

describe('IdentificationDocument', () => {
  it('should create an instance', () => {
    expect(new IdentificationDocument()).toBeTruthy();
  });
});
